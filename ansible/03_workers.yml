#!/usr/bin/env -S ansible-playbook -vv
---
# Phase 03 — Kubernetes Worker Nodes (kubeadm join)
# Steps: load-join-vars → system-prep → containerd → k8s-packages → join → verify

- name: "03-1 | Load join credentials from phase 02"
  hosts: localhost
  gather_facts: false
  become: false
  tasks:
    - name: Load k8s join vars
      ansible.builtin.include_vars:
        file: "{{ playbook_dir }}/output/k8s_join_vars.yml"

    - ansible.builtin.debug:
        msg: "Joining workers to {{ k8s_cp_ip }}:6443"


- name: "03-2 | System prep — worker nodes"
  hosts: workers
  gather_facts: true
  tasks:
    - name: Disable swap (runtime)
      ansible.builtin.command: swapoff -a
      changed_when: false

    - name: Remove swap entries from fstab
      ansible.builtin.lineinfile:
        path: /etc/fstab
        regexp: '.*\bswap\b.*'
        state: absent

    - name: Load required kernel modules
      community.general.modprobe:
        name: "{{ item }}"
        state: present
      loop: [overlay, br_netfilter]

    - name: Persist kernel modules across reboots
      ansible.builtin.copy:
        dest: /etc/modules-load.d/k8s.conf
        content: "overlay\nbr_netfilter\n"
        mode: '0644'

    - name: Apply sysctl settings for Kubernetes
      ansible.posix.sysctl:
        name: "{{ item.key }}"
        value: "{{ item.value }}"
        sysctl_file: /etc/sysctl.d/99-k8s.conf
        reload: true
      loop:
        - { key: net.bridge.bridge-nf-call-iptables,  value: "1" }
        - { key: net.bridge.bridge-nf-call-ip6tables, value: "1" }
        - { key: net.ipv4.ip_forward,                 value: "1" }


- name: "03-3 | Install containerd on worker nodes"
  hosts: workers
  gather_facts: true
  tasks:
    - name: Install apt prerequisites
      ansible.builtin.apt:
        name: [ca-certificates, curl, gnupg, apt-transport-https]
        state: present
        update_cache: true

    - name: Create /etc/apt/keyrings directory
      ansible.builtin.file:
        path: /etc/apt/keyrings
        state: directory
        mode: '0755'

    - name: Add Docker GPG key (containerd source)
      ansible.builtin.shell:
        cmd: >
          curl -fsSL https://download.docker.com/linux/ubuntu/gpg
          | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
        creates: /etc/apt/keyrings/docker.gpg

    - name: Add Docker apt repository
      ansible.builtin.copy:
        dest: /etc/apt/sources.list.d/docker.list
        content: >
          deb [arch={{ 'arm64' if ansible_architecture == 'aarch64' else 'amd64' }}
          signed-by=/etc/apt/keyrings/docker.gpg]
          https://download.docker.com/linux/ubuntu noble stable
        mode: '0644'

    - name: Install containerd.io
      ansible.builtin.apt:
        name: containerd.io
        state: present
        update_cache: true

    - name: Generate default containerd config
      ansible.builtin.shell:
        cmd: containerd config default > /etc/containerd/config.toml
      changed_when: true

    - name: Enable SystemdCgroup in containerd config
      ansible.builtin.replace:
        path: /etc/containerd/config.toml
        regexp: 'SystemdCgroup = false'
        replace: 'SystemdCgroup = true'

    - name: Enable and restart containerd
      ansible.builtin.systemd:
        name: containerd
        enabled: true
        state: restarted


- name: "03-4 | Install kubeadm and kubelet on worker nodes"
  hosts: workers
  gather_facts: false
  vars:
    k8s_minor: "{{ k8s_version.split('.')[:2] | join('.') }}"
  tasks:
    - name: Add Kubernetes GPG key
      ansible.builtin.shell:
        cmd: >
          curl -fsSL
          https://pkgs.k8s.io/core:/stable:/v{{ k8s_minor }}/deb/Release.key
          | gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg
        creates: /etc/apt/keyrings/kubernetes-apt-keyring.gpg

    - name: Add Kubernetes apt repository
      ansible.builtin.copy:
        dest: /etc/apt/sources.list.d/kubernetes.list
        content: >
          deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg]
          https://pkgs.k8s.io/core:/stable:/v{{ k8s_minor }}/deb/ /
        mode: '0644'

    - name: Install kubelet and kubeadm
      ansible.builtin.apt:
        name: [kubelet, kubeadm]
        state: present
        update_cache: true

    - name: Hold Kubernetes packages at installed version
      ansible.builtin.dpkg_selections:
        name: "{{ item }}"
        selection: hold
      loop: [kubelet, kubeadm]

    - name: Enable kubelet
      ansible.builtin.systemd:
        name: kubelet
        enabled: true


- name: "03-5 | Join worker nodes to cluster"
  hosts: workers
  gather_facts: false
  tasks:
    - name: Check if node already joined
      ansible.builtin.stat:
        path: /etc/kubernetes/kubelet.conf
      register: kubelet_conf

    - name: Run kubeadm join
      ansible.builtin.command: "{{ hostvars['localhost']['k8s_join_command'] }}"
      when: not kubelet_conf.stat.exists
      changed_when: true


- name: "03-6 | Verify worker nodes joined the cluster"
  hosts: localhost
  gather_facts: false
  become: false
  tasks:
    - name: Wait for each worker node to be Ready
      ansible.builtin.command: >
        kubectl --kubeconfig {{ kubeconfig }}
        get node {{ item }}
        -o jsonpath='{.status.conditions[?(@.type=="Ready")].status}'
      register: node_check
      until: node_check.stdout == 'True'
      retries: 18
      delay: 10
      loop: [mzcl01-w1, mzcl01-w2]
      changed_when: false

    - name: Show final cluster node status
      ansible.builtin.command: kubectl --kubeconfig {{ kubeconfig }} get nodes -o wide
      register: nodes_out
      changed_when: false

    - ansible.builtin.debug:
        msg: "{{ nodes_out.stdout_lines }}"
